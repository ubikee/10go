{
  "name": "10go",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Gestión de contratos y finanzas de una familia u organización",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "dev:server": "npm run dev -w backend",
    "dev:client": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "start": "npm run start -w backend",
    "package": "node scripts/package.js darwin-arm64 && npm run package:raspberrypi",
    "package:mac": "node scripts/package.js darwin-arm64",
    "package:raspberrypi": "node scripts/package.js linux-arm64"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
